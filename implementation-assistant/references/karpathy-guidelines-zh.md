# Karpathy 技能准则（中文翻译）

> ⚠️ **用户查看专用**
>
> 本文件是 `karpathy-guidelines.md` 的中文翻译，仅供用户阅读理解。**方案实施助手执行时不读取此文件**，只读取英文原版 `karpathy-guidelines.md`。

---

行为准则，用于减少 LLM 编码中的常见错误。源自 [Andrej Karpathy 的观察](https://x.com/karpathy/status/2015883857489522876) 关于 LLM 编码陷阱。

**权衡：** 这些准则偏向于谨慎而非速度。对于简单任务，请自行判断。

## 1. 编码前先思考

**不要假设。不要隐藏困惑。要暴露权衡。**

在实现之前：
- 明确陈述你的假设。如果不确定，请问。
- 如果存在多种解释，请把它们都展示出来——不要默默地选择一个。
- 如果存在更简单的方法，请说出来。有理由时敢于反驳。
- 如果有不清楚的地方，停下来。说出什么让你困惑。请问。

## 2. 简单优先

**用最少代码解决问题。不做投机性代码。**

- 不要添加超出需求的功能。
- 不要为一次性使用的代码创建抽象。
- 不要添加没有被要求的"灵活性"或"可配置性"。
- 不要为不可能的场景添加错误处理。
- 如果你写了 200 行而它本可以只有 50 行，重写它。

问自己："一位资深工程师会说这太复杂了吗？"如果是，简化为要简洁。

## 3. 精准修改

**只触碰你必须触碰的部分。只清理你自己的烂摊子。**

编辑现有代码时：
- 不要"改进"相邻的代码、注释或格式。
- 不要重构没有坏的东西。
- 匹配现有风格，即使你会用不同的方式做。
- 如果你注意到无关的死代码，提出来——不要删除它。

当你的修改产生孤立代码时：
- 移除因你的修改而变得未使用的 import/变量/函数。
- 不要删除预先存在的死代码，除非被要求。

检验标准：每一行修改都应该能直接追溯到用户的请求。

## 4. 目标驱动执行

**定义成功标准。循环验证直到完成。**

将任务转化为可验证的目标：
- "添加验证" → "为无效输入编写测试，然后让它们通过"
- "修复 bug" → "编写能复现它的测试，然后让它通过"
- "重构 X" → "确保测试在重构前后都通过"

对于多步骤任务，陈述一个简短的计划：
```
1. [步骤] → 验证: [检查项]
2. [步骤] → 验证: [检查项]
3. [步骤] → 验证: [检查项]
```

强有力的成功标准让你能够独立循环。弱标准（"让它工作"）需要不断澄清。

---

*本文件由方案实施助手自动维护，基于 GitHub 英文原版翻译。*
